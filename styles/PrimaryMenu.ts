import { motion } from 'framer-motion';
import styled from 'styled-components';

export const PrimaryMenuContainer = styled(motion.div)``;

export const Logo = styled(motion.div)`
  text-align: center;
  padding: 4rem;
  &:hover {
    cursor: pointer;
  }
`;

export const StyledList = styled(motion.ul)`

`